import 'package:flutter/material.dart';
import 'package:true/error_boundary.dart';

class materialsymbolscomputeroutline extends StatelessWidget {
  const materialsymbolscomputeroutline({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: ErrorBoundary(
            child: Container(
      child: Container(
        decoration: BoxDecoration(),
      ),
    )));
  }
}
